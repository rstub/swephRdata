# swephRdata
Data files for swephR. This package is not on CRAN. I order to install it you should use:
```r
install.packages("swephRdata", repos = "https://rstub.r-universe.dev", type = "source")
```
